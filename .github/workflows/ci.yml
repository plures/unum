name: CI
on:
  push:
    branches: [main, "copilot/**", "feature/**"]
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  ci:
    uses: plures/.github/.github/workflows/ci-reusable.yml@main

